#include <bits/stdc++.h> 
int largestElement(vector<int> &arr, int n) {
    // Write your code here.
    int ans=INT_MIN;
    for (int i=0;i<arr.size();i++){
        if(arr[i]>ans){
            ans=max(ans,arr[i]);
        }
    }
    return ans;
    
}
